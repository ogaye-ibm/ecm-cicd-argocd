apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: webhook
spec:
  template:
    metadata:
      name: webhook-jvm-latest
  traffic:
    - tag: jvm
      revisionName: webhook-jvm-latest
      percent: 100